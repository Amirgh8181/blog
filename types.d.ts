type navItem = {
  navItem: string,
  link: string
}

type voidFunc = () => void
type argStr = (arg: string) => void

type loginResponseData={
    status: string,
    token: string,
    username: string,
    id: string,
    email: string
}

type blogCard = {
  label: string,
  icon: React.JSX.Element,
  bgGradiant: string
}


